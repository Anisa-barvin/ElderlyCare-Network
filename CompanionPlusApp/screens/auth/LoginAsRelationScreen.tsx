// LoginAsRelationScreen.tsx
/*
import React from 'react';
import { View, Text, Button, StyleSheet, ScrollView } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const LoginAsRelationScreen: React.FC = () => {
  const navigation = useNavigation();

  return (
    <ScrollView contentContainerStyle={styles.container}>
      <Text style={styles.header}>Login As Relation</Text>

      {}
      <View style={styles.buttonContainer}>
        <Button
          title="Back to Login As Elder"
          onPress={() => navigation.navigate("LoginAsElder" as never)}
        />
        <Button
          title="Register As Elder"
          onPress={() => navigation.navigate("RegisterAsElder" as never)}
        />
        <Button
          title="Register As Relation"
          onPress={() => navigation.navigate("RegisterAsRelation" as never)}
        />
      </View>
    </ScrollView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
    backgroundColor: '#f7f7f7',
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 30,
  },
  buttonContainer: {
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 20,
  },
});

export default LoginAsRelationScreen;
*/